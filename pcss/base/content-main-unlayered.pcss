/*
 * Body / text-formatted links in the primary column: match SODa orange (nav-unlayered).
 * Claro’s a:hover removes underline — keep underline here. Skip .button (link styled as button)
 * and admin dashboard cards (whole card is the control; underline looks like body copy).
 * Skip .soda-scs-manager--card-icon (circular info/book controls: own border + bg).
 */
.scs-manager--content-main a:not(.button):not(.soda-scs-manager--admin-card-link):not(.soda-scs-manager--card-icon) {
  color: #e68500;
  text-decoration: underline;
  text-underline-offset: 0.125em;
}

.scs-manager--content-main a:not(.button):not(.soda-scs-manager--admin-card-link):not(.soda-scs-manager--card-icon):hover,
.scs-manager--content-main a:not(.button):not(.soda-scs-manager--admin-card-link):not(.soda-scs-manager--card-icon):focus {
  color: #b86a00;
  text-decoration: underline;
  text-underline-offset: 0.125em;
}

.scs-manager--content-main a:not(.button):not(.soda-scs-manager--admin-card-link):not(.soda-scs-manager--card-icon):active {
  color: #9a5800;
  text-decoration: underline;
  text-underline-offset: 0.125em;
}
